steps:
  # build the container image
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/${PROJECT_ID}/opencovid2-etl", "."]
  # push the container image
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/${PROJECT_ID}/opencovid2-etl"]
  # migrate Cloud SQL db
  # - name: "gcr.io/google-appengine/exec-wrapper"
  #   args:
  #     [
  #       "-i",
  #       "gcr.io/$PROJECT_ID/django-cloudrun",
  #       "-s",
  #       "${PROJECT_ID}:${_REGION}:opencovid-db",
  #       "--",
  #       "python",
  #       "manage.py",
  #       "migrate",
  #     ]
  # # push static files to Cloud Bucket
  - name: "gcr.io/google-appengine/exec-wrapper"
    args:
      [
        "-i",
        "gcr.io/$PROJECT_ID/opencovid2-etl",
        "-s",
        "${PROJECT_ID}:${_REGION}:opencovid-db",
        "--",
        "python",
        "manage.py",
        "collectstatic",
        "--no-input",
      ]
